.chat-side
  .side-header 
    .side-header__box
      %p.side-header__box_username
        = current_user.name
      %ul.side-header__box_icon
        %li.side-header_box_edituser
          =link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
        %li.side-header_box_newgroup
          = link_to new_group_path do
            = icon('fas', 'edit')
  .group-list
    - current_user.groups.each do |group|
      .group
        = link_to group_ message_path(group) do
          %p.group__name
            =group.name
          %p.group__message
            メッセージはまだありません